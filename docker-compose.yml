version: '3.8'
services:
  autotrader:
    build: .
    container_name: tw_autotrader_bot
    restart: always
    env_file:
      - .env
    volumes:
      - .:/app
    logging:
      driver: "json-file"
      options:
        max-size: "10m" # 防止日誌塞爆 DigitalOcean $6 方案硬碟
        max-file: "3"
