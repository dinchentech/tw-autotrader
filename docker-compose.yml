services:
  autotrader:
    build: .
    container_name: tw_autotrader_bot
    restart: always
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs         # 只持久化 logs，程式碼走 image
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
